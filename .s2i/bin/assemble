#!/bin/bash

chmod 755 /home/jboss

/usr/local/s2i/assemble
rc=$?

cp -f /tmp/src/config/ignite.xml /deployments

find /deployments -name "*-fat.jar" -print -exec ln -sf {} /deployments/${JAVA_APP_JAR:-"app-fat.jar"} \;

exit $rc
