#!/bin/bash

chmod 755 /home/jboss

/usr/local/s2i/assemble
rc=$?

cp -f /tmp/src/config/ignite.xml /deployments

find /deployments -name "*-fat.jar" -exec ln -sf {} /deployments/app-fat.jar \;

exit $rc
